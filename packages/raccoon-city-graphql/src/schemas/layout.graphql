type Mutation {
    createLayout(houseId: String!, file: Upload!, name: String!): FlatLayout
    assignFlatsToLayout(layoutId: String!, flats: [String!]): FlatLayout
}

type Query {
    getLayouts(houseId: String!): [FlatLayout]
}
